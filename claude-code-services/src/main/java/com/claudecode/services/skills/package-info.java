/**
 * Skills system — loads, discovers, and manages skills from multiple sources.
 * Skills are markdown files with YAML frontmatter that provide context to the AI.
 */
package com.claudecode.services.skills;
