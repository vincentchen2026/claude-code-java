/**
 * Claude Code Core module.
 * <p>
 * Contains the core models, types, state management, and query engine.
 * This module defines the fundamental abstractions used across all other modules.
 * <p>
 * Key packages:
 * <ul>
 *   <li>{@code com.claudecode.core.message} — Message type hierarchy (sealed interfaces + records)</li>
 *   <li>{@code com.claudecode.core.state} — Store and AppState management</li>
 *   <li>{@code com.claudecode.core.engine} — QueryEngine and query loop</li>
 *   <li>{@code com.claudecode.core.task} — Task system types and lifecycle</li>
 *   <li>{@code com.claudecode.core.config} — Configuration types</li>
 * </ul>
 */
package com.claudecode.core;
