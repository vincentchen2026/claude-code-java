/**
 * Task system for Claude Code.
 * <p>
 * Manages background tasks (shell commands, sub-agents, workflows).
 * <p>
 * Key types (to be implemented in Task 29):
 * <ul>
 *   <li>{@code TaskType} — task type enum</li>
 *   <li>{@code TaskStatus} — task lifecycle status</li>
 *   <li>{@code TaskStateBase} — task state record</li>
 *   <li>{@code TaskIdGenerator} — unique task ID generation</li>
 * </ul>
 */
package com.claudecode.core.task;
