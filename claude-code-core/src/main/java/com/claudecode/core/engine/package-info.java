/**
 * Query engine for Claude Code.
 * <p>
 * Contains the core AI interaction engine that manages the conversation loop,
 * tool execution, and streaming responses.
 * <p>
 * Key types (to be implemented in Task 4):
 * <ul>
 *   <li>{@code QueryEngine} — core AI interaction engine</li>
 *   <li>{@code QueryEngineConfig} — engine configuration record</li>
 *   <li>{@code QueryMessageIterator} — Virtual Thread + BlockingQueue iterator</li>
 * </ul>
 */
package com.claudecode.core.engine;
