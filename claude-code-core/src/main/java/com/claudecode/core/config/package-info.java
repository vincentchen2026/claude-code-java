/**
 * Configuration types for Claude Code.
 * <p>
 * Multi-level configuration: user → project → local → CLI args.
 * <p>
 * Key types (to be implemented in Task 18):
 * <ul>
 *   <li>{@code AppConfig} — application configuration</li>
 *   <li>{@code ApiConfig} — API client configuration</li>
 * </ul>
 */
package com.claudecode.core.config;
