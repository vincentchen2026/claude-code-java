/**
 * State management for Claude Code.
 * <p>
 * Implements the Store publish-subscribe pattern with immutable state updates.
 * <p>
 * Key types (to be implemented in Task 7):
 * <ul>
 *   <li>{@code Store} — thread-safe publish-subscribe state store</li>
 *   <li>{@code AppState} — application global state record</li>
 * </ul>
 */
package com.claudecode.core.state;
